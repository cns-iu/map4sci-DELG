import { graph } from '../cli.js';
import { startAddingEdges } from './start-adding-edges.js';

// export const addEdgeInterval = setInterval(() => {
//   //startAddingEdges(graph.edgeDistanceOrg);
// }, 5);
